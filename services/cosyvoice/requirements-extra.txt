# CosyVoice3 运行时增量依赖（宿主机已验证 venv 同款版本）
# 不在 uv.lock 内：这些包源码构建依赖 setuptools/pkg_resources/wheel，
# 由 Dockerfile 用 --no-build-isolation 安装（见 requirements-extra 步骤）。
openai-whisper==20231117
wetext==0.0.4
conformer==0.3.2
lightning==2.2.4
hydra-core==1.3.2
rich==13.7.1
matplotlib==3.7.5
gdown==5.1.0
wget==3.2
pyarrow==18.1.0
